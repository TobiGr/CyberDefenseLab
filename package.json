{
  "name": "node-app",
  "version": "1.0.0",
  "description": "Eine einfache Node.js-App für Render",
  "main": "server.js",
  "scripts": {
    "start": "rm -r _site && npx @11ty/eleventy && cd _site && node server.js",
    "serve": "rm -r _site && npx @11ty/eleventy --serve",
    "server": "cd _site && node server.js"
  },
  "author": "Rampe89",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^3.1.0"
  }
}
